import { ExtractorPlugin, Playlist, Song } from "distube";
import type { GuildMember } from "discord.js";
export declare class YtDlpPlugin extends ExtractorPlugin {
    constructor();
    validate(): boolean;
    resolve(url: string, { member, metadata }: {
        member?: GuildMember;
        metadata?: any;
    }): Promise<Playlist<any> | Song<any>>;
    getStreamURL(url: string): Promise<string>;
}
//# sourceMappingURL=index.d.ts.map